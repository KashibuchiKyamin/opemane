server:
  servlet:
    context-path: /opemane
spring:
  datasource:
    driver-class-name: org.h2.Driver
    password: ''
    url: jdbc:h2:mem:testdb
    username: sa
  main:
    banner-mode: 'off'
opemane:
  api:
    baseuri: http://localhost:3001/v1/opemane
    accesstoken:
      id: hoge
      key: fuga
      url: /token
